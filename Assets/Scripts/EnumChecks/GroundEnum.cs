using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GroundEnum : MonoBehaviour
{
    public enum GroundType {Bush, Gravel }
    public GroundType myGround = GroundType.Bush;

}
